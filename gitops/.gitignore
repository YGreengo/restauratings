# .gitignore for GitOps Repository

# =============================================================================
# Helm specific
# =============================================================================
# Chart dependencies (these are downloaded via helm dependency update)
charts/*/charts/
charts/umbrella/charts/*.tgz
**/charts/*.tgz

# Helm lock files (should be committed for reproducible builds)
# charts/*/Chart.lock

# Temporary helm files
**/.helmignore

# =============================================================================
# Terraform specific (if you have terraform alongside GitOps)
# =============================================================================
*.tfstate
*.tfstate.*
*.tfvars.backup
*.tfplan
.terraform/
.terraform.lock.hcl
crash.log
crash.*.log
terraform.rc

# =============================================================================
# Kubernetes & ArgoCD specific
# =============================================================================
# Temporary kubectl files
kubeconfig
*.kubeconfig
kube-config

# ArgoCD generated files
**/kustomization.yaml.bak

# Temporary manifests
generated-manifests.yaml
temp-*.yaml
test-*.yaml

# =============================================================================
# Secrets & Sensitive Data
# =============================================================================
# Environment files with secrets
.env
.env.local
.env.production
.env.staging
*.env

# Secret files (even though they should be in secrets/, be extra safe)
secrets/
**/secrets/
*.secret
*.key
*.crt
*.p12
*.pfx

# PEM files (but allow sealed-secrets public key)
*.pem
!sealed-secrets/sealed-secrets-public.pem

# AWS credentials
.aws/
aws-credentials

# SSH keys
id_rsa
id_rsa.pub
*.ssh
ssh-keys/

# GPG keys
*.gpg
*.asc

# Plaintext secrets - never commit these
*-plain.yaml
plaintext-secrets.txt
*plain.yaml
sealed-secrets/*-plain.yaml
sealed-secrets/plaintext-secrets.txt


# =============================================================================
# IDE & Editor files
# =============================================================================
# VSCode
.vscode/
*.code-workspace

# IntelliJ / PyCharm
.idea/
*.iml
*.iws
*.ipr
out/

# Vim
*.swp
*.swo
*~
.netrwhist

# Emacs
*~
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc

# Sublime Text
*.sublime-workspace
*.sublime-project

# =============================================================================
# OS specific
# =============================================================================
# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~
.directory
.Trash-*

# =============================================================================
# Temporary & Cache files
# =============================================================================
# General temp files
*.tmp
*.temp
*.log
*.cache
temp/
tmp/

# Node modules (if you have any JS tooling)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python cache (if you have any Python tooling)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/

# =============================================================================
# Backup files
# =============================================================================
*.bak
*.backup
*.orig
*.rej

# =============================================================================
# Documentation builds
# =============================================================================
docs/_build/
site/

# =============================================================================
# Custom application files
# =============================================================================
# Add any specific files your team shouldn't commit
debug-*
test-deployment-*
local-*
personal-*

# ArgoCD CLI cache
.argocd/

# Helm plugin cache
.helm/

# Custom scripts that might contain secrets
deploy-local.sh
setup-secrets.sh

# =============================================================================
# GitOps specific - Things you might generate locally for testing
# =============================================================================
# Local testing files
rendered/
output/
manifests-output/

# Local value overrides (keep only the official ones)
values-local.yaml
values-personal.yaml
values-debug.yaml
*-local.yaml
*-personal.yaml

# Local ArgoCD application manifests for testing
test-app-*.yaml
local-app-*.yaml

# Sealed Secrets - exclude temporary files
*-temp.yaml

# Include encrypted sealed secrets (they're safe to commit)
!sealed-secrets/sealed-*.yaml
!sealed-secrets/sealed-secrets-public.pem