apiVersion: v1
kind: Service
metadata:
  name: {{ include "restauratings.fullname" . }}
  labels:
    {{- include "restauratings.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "restauratings.selectorLabels" . | nindent 4 }}